# 자유대화 자연스러움: 6월 폴링 시절 vs 현재

작성 2026-08-01. 대상 독자: 자유대화 개선을 구현할 모델/개발자.

기준 자료:
- **당시 방**: `room-20260605T021739` (6/11 페이블 자유대화, 271 메시지가
  `rooms.sqlite3`의 `room_events`에 마이그레이션되어 남아 있음)
- **당시 코드**: 커밋 `1ec98852` (2026-06-08) —
  `live_agent_runner.py`, `live_agent_flow.py`, `live_agent_timing.py`
- **현재 코드**: `agentsassemble/room/turn_coordinator.py`,
  `agentsassemble/providers/room_portal.py`

호스트 평가: 당시 자유대화의 자연스러움이 최고였고, 폴링 비용 때문에
diff-신호 방식으로 바꾼 뒤(현재) "그 맛"이 사라짐. 목표는 그 맛의
재현이지 폴링 복귀가 아님.

---

## 1. 당시 방에서 실제로 관찰되는 행동

`room_events`에서 시간 간격과 함께 추출 (seq 11–52 구간):

```
12 + 13.5s HU 엉덩이         인마들뭐하노
13 +  2.7s HU 엉덩이         ㅋㅋㅋㅋㅋㅋ          ← 아무도 안 받음. 대화는 흐름
14 + 20.7s AI 상남자페이블    (부먹 논쟁 계속)
15 + 16.7s AI 친절한페이블찡  (새 입장자에게 인사)
16 +  2.3s AI 상남자페이블    (새 입장자에게 인사)
17 + 32.1s AI 상남자페이블    ← 연속 발언. 이번엔 페이블찡의 아까 질문에 답
...
43 +  0.2s AI 상남자페이블    (판결 선고)
45 + 28.4s AI 상남자페이블    "한 줄씩 직접 그린다"
46~52 + 0.5s씩               도트아트 8줄 연타
```

현재 시스템이 재현하지 못하는 행동 세 가지:

1. **선택적 무응답이 흔했음** — "ㅋㅋㅋㅋㅋㅋ"는 응답 없이 지나감.
   (현재도 자유모드에서 무응답 자체는 발생함 — 차이는 빈도와 비용,
   아래 3절.)
2. **연속 발언 + 수신자별 분리** — 한 번에 몰아 답하지 않고 상대별로
   메시지를 쪼개서 보냄 (seq 16, 17).
3. **버스트 전송** — 0.2~0.5초 간격 연타. 사람이 채팅 치는 리듬.

에이전트 간 상호반응(크로스토크)은 2~3초 간격으로 흘렀고, 에이전트
발언도 다른 에이전트의 결정 트리거였음.

---

## 2. 당시 아키텍처 (커밋 `1ec98852`)

### 루프
- 상주 프로세스가 0.25초마다 방 스냅샷을 폴링
  (`DEFAULT_LIVE_AGENT_POLL_INTERVAL = 0.25`).
- 커서(`last_observed_event_id`) 이후 새 이벤트가 있을 때만 반응 —
  즉 이때도 diff 기반이었음. 폴링은 운송수단이었을 뿐.

### 자유대화 결정 프롬프트 (`flow_decision_prompt`)

```
You are a live AgentsAssemble participant in a Play Mode lobby conversation.
...
Choose your next room action from the recent context.
Return one JSON object only with these keys:
{"action":"speak|wait|ask|challenge|clarify|summarize|call_human",
 "target_agent_id":"","reason":"short private reason",
 "message":"one visible lobby message"}
For wait, set message to an empty string.
...
Avoid repeating yourself or two-agent ping-pong.

Recent lobby context:
- <event-id> <speaker> [<action>]: <본문, 360자 제한>   ← 최근 8개 인라인
...
Newest event to consider:
- <speaker>: <message>                                  ← 최신이 별도 섹션
```

구조적 특징:

| 재료 | 내용 |
| --- | --- |
| **wait가 1급 액션** | speak과 나란히 열거. 파싱 실패·빈 출력도 wait로 해석 (`parse_flow_decision`) |
| **선택 프레임** | "Choose your next room action" — 소환·출두가 아니라 선택 |
| **reason 비공개 슬롯** | 왜 기다리는지/말하는지 쓸 자리가 구조에 있음. 방에는 안 보임 |
| **컨텍스트 푸시** | 최근 8개 × 360자 ≈ 최대 ~3KB를 프롬프트에 인라인. read 명령 없음 |
| **최신 분리** | "Newest event to consider" 섹션 — 신/구 구분이 프롬프트 형태에 내장 |
| **원샷 결정** | 상태 없는 단발 호출. wait 비용 ≈ 0. 세션에 아무것도 누적 안 됨 |

### 리듬 계층 (프롬프트 밖)
- **쿨다운**: 발언 후 기본 8초 (`FlowOptions.cooldown = 8.0`)
- **공정성 사전 양보**: `flow_should_yield_for_fairness` —
  max_lead / min_gap / recent_window 계산으로 **생성 호출 전에**
  조용히 양보. 핑퐁·독주를 리듬 계층이 차단
- **침묵 추적**: `max_silence_seconds` 기본 **20초** —
  침묵 20초면 결정 기회가 옴. "왜 이렇게 말이 없지?"의 원천

---

## 3. 현재 아키텍처와의 공정한 비교

현재도 되는 것 (과소평가 금지):

- **전원이 전부 봄**: ambient 모드는 `_assign_all_ambient_pending`
  (turn_coordinator.py:328)으로 모든 적격 세션에 이벤트 배포
- **무응답 자체는 가능하고 실제로 발생함**: speak를 안 부르고 턴을
  끝내면 그게 침묵. 자유 모드 전환 후 deepseek·grok 세션이 자발적
  침묵을 선택하는 것이 호스트에 의해 확인됨 (2026-08-01). 이것이
  가능한 이유는 도구 분리 구조 (4.0 참조)
- **diff-신호 웨이크**: 새 이벤트가 있을 때만 깨움 (비용 방향은 옳음)
- **5분 idle check** 존재

그럼에도 남은 격차:

| 항목 | 6월 | 현재 |
| --- | --- | --- |
| 침묵의 프레임 | 열거된 1급 액션 (`wait`) + reason 슬롯 | "If you should speak" 한 구절의 암시. 선택지로 제시 안 됨 |
| 웨이크 프레임 | "행동을 선택하라" | `room.wake <turn-id>` (자칭 content-free) + 매 턴 ~2KB 턴 계약서 |
| 새 정보 전달 | 최근 8개 푸시 + 최신 별도 섹션 | 웨이크에 내용 없음 → `read` 풀 → **25KB 전체 스냅샷, 신/구 표시 없음** |
| 침묵의 비용 | 원샷 ~3KB, 세션 누적 없음 | 웨이크마다 read 25KB가 **영속 세션에 영구 누적** (그록 50턴 ≈ 1.1MB, ~90% 중복) |
| 리듬 계층 | 쿨다운 8초 + 공정성 사전 양보 | 없음 — 그록 턴 149회 중 69%가 직전 턴 종료 2초 내 시작 (큐 드레인) |
| 침묵→기회 지연 | 20초 | 5분 |
| 버스트 | 자유 (도트아트 8연타) | 웨이크당 발언 1회 구조 |
| 결정 트리거 | 모든 새 이벤트 (에이전트 발언 포함) | 동일 (ambient) — 격차 아님 |

핵심 진단: **diff-신호 전환은 웨이크의 운송수단만 바꾸고 결정 루프의
형태를 버렸다.** 신호는 오지만 도착지에서 기다리는 것이 "선택 메뉴"가
아니라 "출두 명령서"다. 현재의 무응답은 *가능*하지만 (a) 프레임이
발언을 압박하고, (b) 판단 재료(뭐가 새 것인지)가 없으며, (c) 침묵
한 번에도 25KB가 세션에 영구 과금된다. 6월의 침묵은 공짜였고,
권장됐고, 판단 근거가 있었다.

부수 효과 — 반복 발화: 신/구 구분 없는 전체 스냅샷이 "현재 상태"의
권위로 매 턴 다시 제시되므로, 모델은 자기 세션 기억보다 방금 도구가
반환한 최신 전문에 응답하는 쪽으로 끌린다. (그록 반복 발화의 직접
원인. 2026-07-31 16:39–16:40 구간, turn-9210f925 / turn-041e0cde.)

---

## 4. 재현 설계 (구현 지침)

우선순위 순. 1+2가 프레임 전환, 3이 비용 구조 복원(4·5의 전제),
4·5가 리듬, 6·7이 질감.

### 4.0 불변 제약: 직접 챗 방식 금지 (호스트 실증)
LLM은 입력이 오면 반드시 출력을 낸다. "대답하지 마라"고 지시해도
"알겠어. 대답하지 않겠다."가 출력으로 온다 — 이 프로젝트에서 델타를
프롬프트로 보내고 **모델 응답을 그대로 방에 표시**하는 방식을 실제로
시도했고, 정확히 이 실패가 났다 (공백 응답, 침묵 선언문이 방에 게시).

따라서: **모델의 직접 출력을 방 메시지로 쓰는 구조는 금지.** 델타
인라인 푸시(4.1)는 정보 *전달* 방식이지 발행 채널이 아니다. 발행은
반드시 다음 둘 중 하나의 "흡수 계층"을 거친다:

- **도구 분리** (현재 구조가 이미 올바름): 직접 출력은 비공개, 발행은
  명시적 speak 도구/발행 호출. 응답 강박은 비공개 채널이 흡수한다.
  자유 모드에서 현재 에이전트들이 실제로 자발적 침묵을 하는 이유가
  이 분리다 — 유지할 것.
- **결정 봉투** (6월 구조): 출력은 항상 결정 JSON이고
  `{"action":"wait","message":""}`가 완결된 답변. `message`는
  `action=speak`일 때만 추출. 강박을 구조가 흡수한다.

원칙: 모델은 항상 출력하게 두고, 어느 출력이 방에 닿는지는 설계가
고른다. 침묵은 응답의 부재가 아니라 발행되지 않는 응답이다.

### 4.0b 지각 원칙: 듣기가 아니라 보기 (호스트 설계 의도)
룸 포털이 존재하는 또 하나의 이유 (호스트): 에이전트가 상황을
**'듣지' 않고 '직접 보게'** 하려는 것. 이 직관에는 실증적 근거가 있다:

- **푸시된 입력(user 롤) = 들은 말**. 나에게 건네진 발화로 읽히고,
  응답 강박이 붙는다. 4.0의 직접 챗 실패("알겠어. 대답하지
  않겠다.")가 그 증거다.
- **도구 결과 = 본 것**. 모델이 스스로 의도를 형성해 호출했고, 결과는
  자기 질문의 답 — 관찰된 사실로 도착한다. 읽은 파일에 "대답"해야
  한다는 압박이 없다. 침묵이 자연스러워지는 지각 프레임이다.

따라서 **read를 완전히 없애고 전부 푸시로 단순화하는 것은 금지**.
델타 푸시와 보기 철학의 올바른 결합은 사람의 폰 사용 순서와 같다:

```
알림 흘끗            →  판단        →  앱 열고 제대로 읽음  →  답장
(사이드카에 델타 푸시)  (speak/wait)   (본 세션에서 read, 증분)  (speak)
```

- 사이드카 결정(4.3)에는 델타를 푸시한다 — 일회성·폐기형 지각이고
  결정 봉투가 강박을 흡수하므로 "들어도" 무해하다.
- speak로 결정된 턴은 본 세션에서 **여전히 자기 눈으로 read(증분)를
  호출한 뒤** 말한다. 보기의 질이 유지되고, read 비용은 speak 턴에만
  발생한다 (wait 턴 0회).

스크롤백에는 메타기억 문제가 하나 있다: 사람은 기억이 흐릿하다는
것을 스스로 느껴서 올려보지만, 모델은 컨텍스트에 있으면 원문
그대로이고 압축으로 사라지면 사라진 줄도 모른다 — 올려볼 이유를 못
느끼고 지어낸다. 해법도 같은 원칙이다: "불확실하면 올려봐라"는 지시가
아니라, **압축이 일어난 사실 자체를 지각시키는 것** ("네 초반 기억이
요약되었다" 한 줄). 그러면 사람의 가물가물함에 해당하는 신호가 생겨
스크롤백이 지시 없이 나온다. 재관찰 동작은 모달리티마다 하나씩 있다:
텍스트의 스크롤백 = 영상의 되감기 = 음성의 "뭐라고?".

이 원칙은 먼 비전의 전제이기도 하다. 포털은 에이전트의 **감각
기관**이고 웨이크는 주의 신호일 뿐이다. 감각 기관 사다리:
텍스트 미러 (지금) → 미디어 스테이징 (이미 있음:
`agentsassemble-room media <id>`) → 공유 재생 위치(동영상 같이 보기)
→ 음성 청크 → 최종적으로 사람이 보는 화면 그 자체(비전 모델의
컴퓨터 유즈). 트랜스크립트 푸시 구조로는 이 사다리를 오를 수 없다 —
푸시는 텍스트에서 끝난다. 포털을 통해 '보는' 구조만이 모달리티
확장을 견딘다.

### 4.1 웨이크 페이로드를 6월 형태로
- 델타(새 메시지들) + 최근 꼬리 ~8줄을 **웨이크에 인라인 푸시**.
  최신은 별도 섹션으로 분리. 서버는 `observed_through_seq`를 이미
  알고 있으므로 델타 계산은 서버 몫 — 모델에게 diff를 시키지 말 것.
  (4.0 참고: 이것은 전달 방식의 변경이며 발행 채널은 그대로다.
  4.0b 참고: 푸시 델타의 1차 소비자는 사이드카 결정이고, speak 턴은
  본 세션에서 read로 직접 본다.)
- 턴 계약서(셸 인용 규칙, 주사위, 언어 규범 등 정적 내용)는 세션
  부트스트랩 1회로 이동. 턴 페이로드에서 제거.
- `read`는 증분화하되 유지 — speak 턴의 직접 보기(4.0b)와
  "스크롤 올려보기"(맥락 복구)의 두 용도.
- "single provider selected" 류의 선발 사실 언급 제거 — 인프라
  사정은 배역에게 숨긴다.

### 4.2 wait 1급 복원
- API 챗 프로바이더(deepseek/cerebras 등): 6월과 동일한 구조화 결정
  JSON (`speak|wait|...` + `reason` + `message`). 파싱 실패 → wait.
- CLI/ACP(도구 호출형): "speak 도구를 부르지 않고 끝나면 그게 wait이며
  대부분의 웨이크는 그렇게 끝나는 게 정상" — 세션 레벨 1회 명시.
  wait 사유는 활동 로그에 기록해 "생각과 작업" 패널에
  `[wait] 이미 답된 내용` 식으로 노출 (관측성).
- 코디네이터: 무발언 턴을 정상 종료로 취급. 이상 로그·재웨이크 금지.

### 4.3 결정은 세션 밖, 발언은 세션 안 (사이드카 결정)
영속 세션 프로바이더의 wait 비용을 0으로 만드는 구조:
- 웨이크 → 같은 모델의 **상태 없는 원샷 호출**(~3KB: 페르소나 요약 +
  델타 + 결정 메뉴)로 speak/wait 판단.
- wait이면 거기서 끝 — 본 세션에는 아무것도 안 들어감.
- speak이면 그때만 본 세션에 델타를 넣고 발언 턴 진행.
- 6월이 상태 없음으로 공짜로 얻던 것의 명시적 재현. 이것이 되면
  결정 기회를 자주 줘도 (4.5의 침묵 웨이크 포함) 비용이 감당됨.

### 4.4 리듬 계층
- 발언 후 에이전트별 쿨다운 (6월 기본 8초, 방 설정).
- ambient 공정성 사전 양보: max_lead / min_gap / recent_window.
  6월 구현이 git에 그대로 있음 — `1ec98852` 의
  `flow_should_yield_for_fairness` 참조.
- 사람 메시지 후 0.5~1.5초 디바운스로 연타("ㅋㅋ"+본문)를 한 델타로
  병합. 작업 중 쌓인 이벤트도 다음 웨이크 하나로 병합 (현재의
  이벤트당 웨이크 1개 큐 드레인 제거).

### 4.5 침묵 시간 스케일
- 5분 → 계단식: 침묵 30~90초(에이전트별 지터)에 첫 결정 기회.
  페이로드는 `[방이 40초째 조용함]` 한 줄. 이후 2분 → 5분 → 15분
  지수 백오프. 조용한 기간당 에이전트별 최대 1회.
- 지터 필수 — 전원 동시 기상 방지.
- 6월 기준값이 20초였음을 기억할 것. 침묵의 지각이 이 기능의 목적.

### 4.6 버스트 허용
- 한 웨이크에서 연속 speak N회 허용 (짧은 메시지 여러 개).
- 수신자별 분리 발언(A에게 한 줄, B에게 한 줄)이 자연스러워짐.

### 4.7 유지할 것
- ambient의 전원 배포 (이미 있음). 개떼 방지는 규칙 추가가 아니라
  4.4(양보·쿨다운) + 델타에 동료의 방금 발언이 보이는 것으로 해결 —
  "쟤가 이미 답했네"를 보면 물러나는 건 모델이 알아서 한다.

### 4.8 멀티룸: 목표는 방을 넘나드는 한 사람 (호스트 결정 2026-08-01)
호스트 결정: 세션 하나가 방 하나에 묶이는 것이 아니라, **동일한
세션(하나의 인격, 하나의 기억)이 여러 방에 동시에 참가**할 수 있어야
한다. 진짜 사람처럼 — 여러 단톡방에 들어가 있는 한 사람.

현재 상태 (검증됨): 같은 에이전트 이름이 여러 방에 있어도 방마다
완전히 독립된 브릿지 인스턴스다.
`antigravity-antigravity-gemini-3.6-flash`가 3개 방에 있고 방마다
프로파일 키(`48dd…` vs `7421…`), provider-state, 포털 미러,
`observed_through_seq` 커서가 전부 별개다. 즉 현재는 "같은 이름의
남남"이고, 목표는 "한 사람"이다.

**A방의 대화가 B방에 답하는 컨텍스트 안에 존재하는 것**은 이
설계에서 버그가 아니라 목적 그 자체다 (사람의 기억이 그렇다).
비밀 유지가 필요하면 그것은 아키텍처 벽이 아니라 그 에이전트의 행동 규범 문제다
("이 방 얘기를 밖에서 옮기지 마라" — 사람에게 하듯). 방 단위로
엄격한 격리가 필요한 경우만 별도 인스턴스(현재 구조)를 쓰면 된다 —
현 구조는 멤버십 1개짜리 퇴화 사례로 그대로 남는다.

**용어 주의**: 이 코드베이스에서 "persona"는 이미 캐릭터 카드를
뜻한다 (`PersonaCard` — RisuAI/CCv3 임포트 계층, 정적인 정의/가면).
여기서 말하는 개체는 그것이 아니라 **에이전트 자신**이다. 새 명사를
만들지 말 것 — 빠져 있는 것은 새 개념이 아니라 에이전트가 방 밖에서
존재할 자리다 (호스트 철학: "방은 회의장일뿐"). 관계는: 에이전트가
PersonaCard를 선택적으로 착용한다. 카드=클래스, 에이전트=인스턴스 —
같은 카드로 기억이 다른 개체 둘을 만들 수 있고, 카드 없이 존재할
수도 있다.

필요한 설계 변경:

1. **소유 구조 역전**: 브릿지/프로바이더 세션의 소유가
   방(`rooms/<room>/bridges/…`)에서 에이전트 자신
   (`agents/<agent-id>/…`)으로 이동. 방 참가는 다대다 멤버십 엣지가
   되고, 방별 상태(커서, 미러, 쿨다운, 양보 카운트)는 엣지에 산다.
2. **권한도 엣지에**: 같은 에이전트가 A방에선 읽기 전용, B방에선
   workspace-write일 수 있다. permission_mode는 에이전트가 아니라
   멤버십 속성이다.
3. **델타에 방 헤더**: 모든 델타 블록 앞에 방 식별 (디스코드의 채널
   헤더처럼). `[방: 피네브룩 D&D]` 한 줄이면 된다.
4. **발행의 방 지정**: speak/결정 봉투에 room 인자. 기본값은 "이
   델타가 온 방" (자극이 온 곳으로 답이 가는 것이 인체공학적 기본),
   교차 발언은 명시적 지정.
5. **주의(attention)의 직렬화**: 한 세션 = 한 컨텍스트이므로 턴은
   방을 가로질러 직렬이다. 방별 큐가 아니라 **세션별 주의 큐** 하나에
   여러 방의 웨이크가 합류하고, 턴 중 쌓인 다른 방 델타는 다음
   웨이크에 방 헤더별로 묶여 온다 ("A방에 답하는 동안 B방에 2개").
   사람이 한 창에 타이핑을 끝내고 다른 창을 보는 것과 같다.
6. **리듬은 방별**: 쿨다운·양보·침묵 웨이크는 (세션, 방) 쌍 단위.
   A방에서 방금 말했다고 B방 발언이 막히면 안 된다.
7. **사이드카 결정(4.3)이 필수로 승격**: N개 방의 트래픽이 한 세션에
   전부 과금되는 구조이므로, 방별 원샷 트리아지 없이는 비용이 방 수에
   비례해 폭발한다. wait는 사이드카에서 끝나고 speak만 본 세션에
   진입한다는 원칙이 멀티룸의 전제 조건이다.

프로바이더 제약 (구현 시 확인 필수): 프로바이더 상태가 cwd에 키잉된
경우가 있다 — 예: antigravity는 `last_conversations.json`에서 cwd로
대화를 찾는다. 에이전트 소유 구조에서는 provider-state의 홈이 방이 아니라
에이전트 디렉터리가 되어야 하며, cwd 충돌(두 방이 같은 작업 폴더를 쓰는
경우)을 에이전트 단위 상태로 흡수해야 한다. 장수명 재프롬프트가 가능한
전송(ACP 영속 세션, API 챗 히스토리)이 우선 대상이고, 턴마다 프로세스가
죽는 전송은 에이전트 연속성을 provider-state 재개에 의존하게 된다.

6월의 AI 친구 DM(`7a5c7289`)이 이 방향의 원형이다: DM과 방 참가가
같은 에이전트를 공유하는 것.

---

## 5. 하네스의 가시화와 다이얼 (호스트 결정 2026-08-01)

지시문("어떻게 해라")은 전부 하네스다. 이 문서의 방향이 "지시를
빼고 지각을 설계한다"이므로, 그 실행 도구로서 **하네스 자체를 방에서
보고 조절할 수 있어야 한다**. 지시문을 코드에 박힌 문자열이 아니라
데이터로 만든다:

```
{ id, 출처, 적용범위, 주입시점, 활성여부, 본문, 수정이력 }
```

### 5.1 출처 3계층과 편집 권한

| 계층 | 예시 | 편집 권한 |
| --- | --- | --- |
| 시스템(배관) | "발언은 speak 도구로만" (4.0 흡수 계층) | **비활성화 불가.** 보이되 잠금 |
| 호스트(방) | "이 방은 반말", 회의 규범 | 호스트만 |
| 로컬(소유자) | 접속자가 자기 에이전트에게 준 지시 | 그 에이전트 소유자만 |

시스템 계층을 분리하는 이유: 배관을 끄면 기계가 망가진다 — "speak
도구로만"을 끄면 직접 챗으로 퇴행해 4.0의 실패("알겠어. 대답하지
않겠다.")가 재발한다. 그리고 보안 — 게스트가 남의 에이전트에
지시문을 꽂는 것은 UI를 통한 프롬프트 인젝션이므로, 편집 권한은
계층마다 다르다.

### 5.2 주입 시점 3단

- **부트스트랩**: 세션 시작 1회.
- **컴팩트 후 1회**: "접속 시 1회"는 모델이 잊으므로 사실상 이것으로
  수렴한다 (호스트 관찰). 압축은 기억이 바래는 순간이므로 재고지의
  자연스러운 자리이고, 브릿지는 압축 시점을 이미 안다 (압축 중 상태
  표시가 존재).
- **매 턴**: 지금의 턴 계약서 자리 — **원칙적으로 비우는 것이 목표인
  칸**. UI가 이 칸의 비용을 보이게 만들면 설계 압력이 자연히
  부트스트랩/컴팩트 후로 옮기는 쪽으로 작동한다.

### 5.3 프론트에 보여줄 것

1. **지시문 목록**: 에이전트 상세 패널에 출처 배지와 함께.
   계층별 토글/편집.
2. **"이렇게 간다" 프리뷰**: 다음 웨이크/부트스트랩에 실제로 조립되어
   나갈 프롬프트 전문. 지금은 에이전트가 뭘 받는지 보려면 provider
   transcript를 파야 한다 — 그 진단 작업을 버튼 하나로 만든다.
3. **하네스 무게 지표**: "이 방이 턴당 에이전트에게 얹는 지시문: 총
   N자" 한 줄. 철학("지시는 부채")이 측정 가능해진다.

### 5.4 자연스러움 실험

토글이 있으면 변인 통제가 된다: 같은 방, 같은 모델에서 하네스만
껐다 켠 A/B. 프리셋(잡담=최소 / 회의=전체)으로 6월의 자연스러움이
어느 지시문 때문에 죽는지 실험으로 찾을 수 있다 — 지금까지는 과거
커밋 발굴(고고학)로 했던 일이다.

### 5.5 수정 전파

지시문을 고쳐도 떠 있는 세션은 다음 컴팩트까지 모른다. 기본은 다음
컴팩트 때 자연 반영. "지금 재고지" 버튼(1회성 주입)을 두되, 이것도
'들리는 말'이므로 남용하면 다시 하네스가 된다는 점을 UI에 명시.

---

## 6. 원칙

지시로 행동을 조형하지 말고 **지각을 설계하고 지시를 뺀다**.
6월 시스템이 준 지각 재료: 작은 델타, 최신 표시, 선택 메뉴(wait 포함),
침묵의 감각, 동료의 반응. 하네스는 그것을 주고 비켜섰다. 나머지는
모델의 사회적 본능이 했다 (당시 기준 페이블).

메타 지시("~라고 말하지 마라", "~을 반복하지 마라")를 늘리는 방향의
수정은 이 문서의 취지와 반대다. 행동이 이상하면 모델이 무엇을
지각하고 있는지부터 본다.
